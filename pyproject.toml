[project]
name = "swagger-petstore"
version = "0.1.0"
description = "Auto-generated MCP adapter for Swagger Petstore"
requires-python = ">=3.11"
dependencies = [
    "dedalus-mcp>=0.7.0",
    "httpx>=0.27.0",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends._legacy:_Backend"
